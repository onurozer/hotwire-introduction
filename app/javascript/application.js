// Entry point for the build script in your package.json
import "@hotwired/turbo-rails";
import "./controllers";

// Disable Turbo
// Turbo.session.drive = false;

// Displays a progress bar during turbo navigation
// Turbo.setProgressBarDelay(1);
